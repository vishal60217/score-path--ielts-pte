let content=null;
const $=s=>document.querySelector(s); const $$=s=>[...document.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
async function api(url,opt={}){const r=await fetch(url,{headers:{'Content-Type':'application/json',...(opt.headers||{})},...opt});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Something went wrong');return d;}
function toast(m,k='info'){const t=document.createElement('div');t.className='toast '+k;t.textContent=m;document.body.appendChild(t);setTimeout(()=>t.remove(),3500)}
function show(v){$$('.view').forEach(x=>x.classList.remove('active'));$('#'+v)?.classList.add('active');$$('.nav-btn').forEach(b=>b.classList.toggle('active',b.dataset.view===v));scrollTo({top:0,behavior:'smooth'});if(v==='courses')renderCourses();if(v==='live')renderClasses();if(v==='tests')renderTest();if(v==='pricing')renderPlans();if(v==='dashboard')loadDashboard();if(v==='admin')loadAdmin();}
function openModal(h){$('#modalBody').innerHTML=h;$('#modal').classList.remove('hidden')}function closeModal(){$('#modal').classList.add('hidden')}window.closeModal=closeModal;
document.addEventListener('click',e=>{const v=e.target.closest('[data-view]');if(v){e.preventDefault();show(v.dataset.view)}const f=e.target.closest('[data-filter]');if(f){$$('.filter').forEach(x=>x.classList.remove('active'));f.classList.add('active');renderCourses(f.dataset.filter)}const l=e.target.closest('[data-lesson]');if(l)openLesson(l.dataset.lesson);const p=e.target.closest('.subscribe');if(p)subscribePlan(p.dataset.plan);if(e.target.closest('#logoutBtn'))logout()});
async function init(){ $('#year').textContent=new Date().getFullYear();content=await api('/api/content');renderCourses();renderClasses();renderTest();renderPlans();await updateAccount();}
function renderCourses(filter='All'){const list=(content.courses||[]).filter(c=>filter==='All'||c.exam===filter);$('#courseGrid').innerHTML=list.map(c=>`<article class="course-card"><div class="course-top"><span class="pill">${esc(c.exam)}</span><span>${c.lessons??0} lessons</span></div><h3>${esc(c.title)}</h3><p>${esc(c.description)}</p><div class="chips">${(c.skills||[]).map(s=>`<span>${esc(s)}</span>`).join('')}</div><button class="secondary" data-view="pricing">Get access</button></article>`).join('')||'<p>No courses published yet.</p>'}
function renderClasses(){const teachers=content.teachers||[];$('#classGrid').innerHTML=(content.classes||[]).map(c=>`<article class="class-card"><span class="pill">${esc(c.exam)}</span><h3>${esc(c.title)}</h3><p><b>${esc(c.date)}</b><br>${esc(c.time)} • ${esc(c.instructor)}</p><p>${esc(c.description||'Live guided practice and Q&A.')}</p>${c.link?`<a class="primary inline" href="${esc(c.link)}" target="_blank" rel="noopener">Join session</a>`:'<span class="meta">Meeting link will appear when published.</span>'}</article>`).join('')||'<p>No live classes published yet.</p>'}
function renderPlans(){ $('#pricingGrid').innerHTML=(content.pricing||[]).map(p=>`<article class="price-card ${p.featured?'featured':''}">${p.featured?'<div class="tag">Best value</div>':''}<h3>${esc(p.name)}</h3><div class="price">₹${p.price}<small> / ${p.days} days</small></div><ul>${(p.features||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><button class="primary subscribe" data-plan="${esc(p.id)}">Choose ${esc(p.name)}</button></article>`).join('')||'<p>No membership plans available.</p>'}
function renderTest(){const qs=content.questions||[];$('#testBox').innerHTML=qs.map((q,i)=>`<div class="test-q"><b>${i+1}. ${esc(q.prompt)}</b>${q.options.map((o,j)=>`<label><input type="radio" name="${q.id}" value="${j}"> ${esc(o)}</label>`).join('')}</div>`).join('')+'<button class="primary" id="testSubmit">Submit diagnostic</button><div id="testResult"></div>';$('#testSubmit').onclick=submitTest}
async function submitTest(){const answers={};(content.questions||[]).forEach(q=>{const x=document.querySelector(`input[name="${q.id}"]:checked`);if(x)answers[q.id]=+x.value});try{const d=await api('/api/test/submit',{method:'POST',body:JSON.stringify({answers})});$('#testResult').innerHTML=`<div class="result"><strong>${d.score}/${d.total}</strong> — ${d.percent}%<br>${esc(d.message)}</div>`}catch(e){openLogin('Create a free account to save your diagnostic result.')}}
async function updateAccount(){const d=await api('/api/me');const b=$('#accountBtn');b.textContent=d.user?(d.user.role==='admin'?'Admin':'Dashboard'):'Login';b.onclick=()=>d.user?show(d.user.role==='admin'?'admin':'dashboard'):openLogin();$('#heroCta').textContent=d.user?'Open dashboard':'Start learning';$('#heroCta').onclick=()=>d.user?show('dashboard'):openRegister()}
async function loadDashboard(){try{const d=await api('/api/dashboard');const s=d.subscription;const days=s?Math.max(0,Math.ceil((new Date(s.expiresAt||s.expires_at)-Date.now())/86400000)):0;$('#dash').innerHTML=`<div class="dash-grid"><div class="dash-card"><span>Student</span><h3>${esc(d.user.name)}</h3><p>${esc(d.user.email)}</p></div><div class="dash-card"><span>Membership</span><h3>${s?esc(s.planName):'Free account'}</h3><p>${s?`Active • ${days} day${days===1?'':'s'} left • expires ${new Date(s.expiresAt||s.expires_at).toLocaleDateString()}`:'Subscribe to unlock your courses.'}</p><button class="secondary" data-view="pricing">${s?'Renew / extend':'Choose a plan'}</button></div><div class="dash-card"><span>Progress</span><h3>${d.progress.completed}/${d.progress.total}</h3><p>Lessons completed</p></div></div><div class="dashboard-section"><h3>Your courses</h3><div class="mini-grid">${d.courses.map(c=>`<div class="mini-card"><span class="pill">${esc(c.exam)}</span><h4>${esc(c.title)}</h4><p>${c.access?'🔓 Included in your membership':'🔒 Not included'}</p>${c.access?`<button class="secondary" data-lesson="${esc(c.firstLessonId||'')}" ${c.firstLessonId?'':'disabled'}>Open lesson</button>`:'<button class="secondary" data-view="pricing">Upgrade</button>'}</div>`).join('')}</div></div><button class="secondary" id="logoutBtn">Log out</button>`}catch(e){openLogin()}}
async function openLesson(id){if(!id)return;try{const l=await api('/api/lessons/'+encodeURIComponent(id));openModal(`<span class="pill">${esc(l.exam)} • ${esc(l.skill)}</span><h2>${esc(l.title)}</h2><p class="lesson-body">${esc(l.body)}</p><h3>What you'll learn</h3><ul>${(l.objectives||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul><button class="primary" onclick="markComplete('${esc(l.id)}')">Mark complete</button>`)}catch(e){if(/log in/i.test(e.message))openLogin();else if(/membership|include/i.test(e.message))openModal(`<h2>Membership required</h2><p>${esc(e.message)}</p><button class="primary" onclick="closeModal();show('pricing')">View plans</button>`);else toast(e.message,'error')}}
async function markComplete(id){try{await api('/api/progress',{method:'POST',body:JSON.stringify({lessonId:id})});closeModal();toast('Lesson completed.','success');loadDashboard()}catch(e){toast(e.message,'error')}}
function openLogin(note=''){openModal(`<h2>Welcome back</h2>${note?`<p class="notice">${esc(note)}</p>`:''}<div class="field"><label>Email</label><input id="loginEmail" type="email"></div><div class="field"><label>Password</label><input id="loginPass" type="password"></div><button class="primary" onclick="login()">Login</button><p class="meta">New here? <a href="#" onclick="openRegister();return false">Create account</a></p>`)}
function openRegister(){openModal(`<h2>Create your free account</h2><div class="field"><label>Full name</label><input id="regName"></div><div class="field"><label>Email</label><input id="regEmail" type="email"></div><div class="field"><label>Password (8+ characters)</label><input id="regPass" type="password"></div><button class="primary" onclick="register()">Create account</button><p class="meta">Already registered? <a href="#" onclick="openLogin();return false">Login</a></p>`)}
async function login(){try{await api('/api/login',{method:'POST',body:JSON.stringify({email:$('#loginEmail').value,password:$('#loginPass').value})});closeModal();await updateAccount();show('dashboard')}catch(e){toast(e.message,'error')}}
async function register(){try{await api('/api/register',{method:'POST',body:JSON.stringify({name:$('#regName').value,email:$('#regEmail').value,password:$('#regPass').value})});closeModal();await updateAccount();show('dashboard')}catch(e){toast(e.message,'error')}}
async function logout(){await api('/api/logout',{method:'POST'});await updateAccount();show('home');toast('Logged out','success')}
async function subscribePlan(plan){try{const d=await api('/api/subscribe',{method:'POST',body:JSON.stringify({plan})});if(d.mode==='razorpay'){const rzp=new Razorpay({key:d.keyId,amount:d.amount,currency:'INR',name:'ScorePath',description:d.planName,order_id:d.orderId,prefill:d.prefill,theme:{color:'#111827'},handler:async response=>{try{await api('/api/payment/verify',{method:'POST',body:JSON.stringify({...response,plan})});toast('Payment successful — membership activated.','success');await updateAccount();show('dashboard')}catch(e){toast(e.message,'error')}}});rzp.open()}else openModal(`<h2>Pay by Bank / UPI</h2><p>${esc(d.instructions)}</p><div class="bank-box">${d.bank.bankName?`<b>Bank:</b> ${esc(d.bank.bankName)}<br>`:''}${d.bank.accountName?`<b>Account name:</b> ${esc(d.bank.accountName)}<br>`:''}${d.bank.accountNumber?`<b>Account number:</b> ${esc(d.bank.accountNumber)}<br>`:''}${d.bank.ifsc?`<b>IFSC:</b> ${esc(d.bank.ifsc)}<br>`:''}${d.bank.upiId?`<b>UPI ID:</b> ${esc(d.bank.upiId)}<br>`:''}<b>Amount:</b> ₹${d.amount}</div><div class="field"><label>UTR / transaction number</label><input id="utr"></div><button class="primary" onclick="submitBankPayment('${esc(plan)}')">I have paid</button>`)}catch(e){const me=await api('/api/me');if(!me.user)openLogin('Create an account before purchasing.');else toast(e.message,'error')}}
async function submitBankPayment(plan){try{await api('/api/payment/manual',{method:'POST',body:JSON.stringify({plan,utr:$('#utr').value})});closeModal();toast('Payment submitted. Access will activate after admin verification.','success')}catch(e){toast(e.message,'error')}}
function adminFormData(d){return `<div class="admin-tabs"><button class="secondary" onclick="adminSection('overview')">Overview</button><button class="secondary" onclick="adminSection('courses')">Courses</button><button class="secondary" onclick="adminSection('lessons')">Lessons</button><button class="secondary" onclick="adminSection('plans')">Plans</button><button class="secondary" onclick="adminSection('teachers')">Teachers</button><button class="secondary" onclick="adminSection('classes')">Live classes</button><button class="secondary" onclick="adminSection('payments')">Payments</button><button class="secondary" onclick="adminSection('settings')">Bank & settings</button></div><div id="adminWorkspace"></div>`}
let adminData=null;
async function loadAdmin(){try{adminData=await api('/api/admin/overview');$('#adminPanel').innerHTML=adminFormData(adminData);adminSection('overview')}catch(e){toast(e.message,'error')}}
function adminSection(s){const w=$('#adminWorkspace');if(!w)return;if(s==='overview')w.innerHTML=`<div class="dash-grid"><div class="dash-card"><span>Students</span><h3>${adminData.stats.students}</h3></div><div class="dash-card"><span>Active memberships</span><h3>${adminData.stats.activeSubscriptions}</h3></div><div class="dash-card"><span>Pending payments</span><h3>${adminData.stats.pendingPayments}</h3></div><div class="dash-card"><span>Lessons</span><h3>${adminData.stats.lessons}</h3></div></div>`;if(s==='payments')w.innerHTML=`<div class="admin-panel"><h3>Pending manual payments</h3>${adminData.pendingPayments.length?adminData.pendingPayments.map(p=>`<div class="admin-row"><div><b>${esc(p.userName)}</b><br>₹${p.amount} • ${esc(p.planName)}<br>UTR: ${esc(p.utr)}</div><div><button class="primary" onclick="approvePayment('${p.id}')">Approve</button> <button class="secondary" onclick="rejectPayment('${p.id}')">Reject</button></div></div>`).join(''):'<p>No pending payments.</p>'}</div>`;if(s==='courses')w.innerHTML=`<div class="admin-panel"><h3>Add / edit course</h3><div class="form-grid"><input id="cId" placeholder="ID (blank = new)"><input id="cExam" placeholder="IELTS / PTE / Academic"><input id="cTitle" placeholder="Course title"><input id="cSkills" placeholder="Skills comma separated"><textarea id="cDesc" placeholder="Description"></textarea></div><button class="primary" onclick="saveCourse()">Save course</button></div><div class="admin-panel"><h3>Published courses</h3>${adminData.courses.map(c=>`<div class="admin-row"><div><b>${esc(c.title)}</b><br>${esc(c.exam)} • ${(c.skills||[]).map(esc).join(', ')}</div><button class="secondary" onclick='editCourse(${JSON.stringify(c)})'>Edit</button> <button class="secondary" onclick="deleteItem('course','${c.id}')">Delete</button></div>`).join('')}</div>`;if(s==='lessons')w.innerHTML=`<div class="admin-panel"><h3>Add / edit lesson</h3><div class="form-grid"><input id="lId" placeholder="ID (blank = new)"><select id="lCourse">${adminData.courses.map(c=>`<option value="${c.id}">${esc(c.title)}</option>`).join('')}</select><input id="lExam" placeholder="Exam"><input id="lSkill" placeholder="Skill"><input id="lTitle" placeholder="Lesson title"><textarea id="lBody" placeholder="Lesson content"></textarea><input id="lObj" placeholder="Objectives comma separated"></div><button class="primary" onclick="saveLesson()">Save lesson</button></div><div class="admin-panel"><h3>Lessons</h3>${adminData.lessons.map(l=>`<div class="admin-row"><div><b>${esc(l.title)}</b><br>${esc(l.exam)} • ${esc(l.skill)}</div><button class="secondary" onclick='editLesson(${JSON.stringify(l)})'>Edit</button> <button class="secondary" onclick="deleteItem('lesson','${l.id}')">Delete</button></div>`).join('')}</div>`;if(s==='plans')w.innerHTML=`<div class="admin-panel"><h3>Add / edit membership plan</h3><div class="form-grid"><input id="pId" placeholder="ID (blank = new)"><input id="pName" placeholder="Plan name"><input id="pPrice" type="number" placeholder="Price INR"><input id="pDays" type="number" placeholder="Days"><input id="pFeatures" placeholder="Features comma separated"><input id="pCourses" placeholder="Course IDs comma separated"><label><input id="pFeatured" type="checkbox"> Featured</label></div><button class="primary" onclick="savePlan()">Save plan</button></div><div class="admin-panel"><h3>Plans</h3>${adminData.plans.map(p=>`<div class="admin-row"><div><b>${esc(p.name)}</b> — ₹${p.price}/${p.days} days<br>${(p.courseIds||[]).join(', ')}</div><button class="secondary" onclick='editPlan(${JSON.stringify(p)})'>Edit</button> <button class="secondary" onclick="deleteItem('plan','${p.id}')">Delete</button></div>`).join('')}</div>`;if(s==='teachers')w.innerHTML=`<div class="admin-panel"><h3>Add / edit teacher</h3><div class="form-grid"><input id="tId" placeholder="ID"><input id="tName" placeholder="Name"><input id="tPhoto" placeholder="Photo URL"><textarea id="tBio" placeholder="Bio"></textarea></div><button class="primary" onclick="saveTeacher()">Save teacher</button></div><div class="admin-panel"><h3>Teachers</h3>${adminData.teachers.map(t=>`<div class="admin-row"><div><b>${esc(t.name)}</b><br>${esc(t.bio)}</div><button class="secondary" onclick='editTeacher(${JSON.stringify(t)})'>Edit</button> <button class="secondary" onclick="deleteItem('teacher','${t.id}')">Delete</button></div>`).join('')}</div>`;if(s==='classes')w.innerHTML=`<div class="admin-panel"><h3>Add / edit live class</h3><div class="form-grid"><input id="clId" placeholder="ID"><input id="clTitle" placeholder="Class title"><input id="clExam" placeholder="IELTS / PTE / Both"><input id="clDate" placeholder="Date"><input id="clTime" placeholder="Time"><input id="clInstructor" placeholder="Instructor"><input id="clTeacher" placeholder="Teacher ID"><input id="clLink" placeholder="Zoom / Meet link"><textarea id="clDesc" placeholder="Description"></textarea></div><button class="primary" onclick="saveClass()">Save class</button></div><div class="admin-panel"><h3>Published classes</h3>${adminData.classes.map(c=>`<div class="admin-row"><div><b>${esc(c.title)}</b><br>${esc(c.date)} • ${esc(c.time)} • ${esc(c.instructor)}</div><button class="secondary" onclick='editClass(${JSON.stringify(c)})'>Edit</button> <button class="secondary" onclick="deleteItem('class','${c.id}')">Delete</button></div>`).join('')}</div>`;if(s==='settings')w.innerHTML=`<div class="admin-panel"><h3>Bank / UPI and site settings</h3><p class="meta">These details are shown to customers choosing bank/UPI payment. Do not put passwords or API secrets here.</p><div class="form-grid"><input id="sBank" value="${esc(adminData.bank.bankName||'')}" placeholder="Bank name"><input id="sAccount" value="${esc(adminData.bank.accountName||'')}" placeholder="Account name"><input id="sNumber" value="${esc(adminData.bank.accountNumber||'')}" placeholder="Account number"><input id="sIfsc" value="${esc(adminData.bank.ifsc||'')}" placeholder="IFSC"><input id="sUpi" value="${esc(adminData.bank.upiId||'')}" placeholder="UPI ID"><input id="sEmail" value="${esc(adminData.bank.supportEmail||'')}" placeholder="Support email"><input id="sSite" value="${esc(adminData.bank.siteName||'ScorePath')}" placeholder="Site name"></div><button class="primary" onclick="saveSettings()">Save settings</button></div>`}
function fill(ids,o){Object.entries(ids).forEach(([id,key])=>{if($(id))$(id).value=o[key]??''})}
function editCourse(c){fill({'#cId':'id','#cExam':'exam','#cTitle':'title','#cSkills':'skills','#cDesc':'description'},{...c,skills:(c.skills||[]).join(',')})}
function editLesson(l){fill({'#lId':'id','#lCourse':'courseId','#lExam':'exam','#lSkill':'skill','#lTitle':'title','#lBody':'body','#lObj':'objectives'},{...l,objectives:(l.objectives||[]).join(',')})}
function editPlan(p){fill({'#pId':'id','#pName':'name','#pPrice':'price','#pDays':'days','#pFeatures':'features','#pCourses':'courseIds'},{...p,features:(p.features||[]).join(','),courseIds:(p.courseIds||[]).join(',')});$('#pFeatured').checked=!!p.featured}
function editTeacher(t){fill({'#tId':'id','#tName':'name','#tPhoto':'photoUrl','#tBio':'bio'},t)}
function editClass(c){fill({'#clId':'id','#clTitle':'title','#clExam':'exam','#clDate':'date','#clTime':'time','#clInstructor':'instructor','#clTeacher':'teacherId','#clLink':'link','#clDesc':'description'},c)}
async function saveCourse(){await adminPost('/api/admin/course',{x:{id:$('#cId').value||undefined,exam:$('#cExam').value,title:$('#cTitle').value,skills:$('#cSkills').value.split(',').map(x=>x.trim()).filter(Boolean),description:$('#cDesc').value}});}
async function saveLesson(){await adminPost('/api/admin/lesson',{x:{id:$('#lId').value||undefined,courseId:$('#lCourse').value,exam:$('#lExam').value,skill:$('#lSkill').value,title:$('#lTitle').value,body:$('#lBody').value,objectives:$('#lObj').value.split(',').map(x=>x.trim()).filter(Boolean)}})}
async function savePlan(){await adminPost('/api/admin/plan',{x:{id:$('#pId').value||undefined,name:$('#pName').value,price:+$('#pPrice').value,days:+$('#pDays').value,features:$('#pFeatures').value.split(',').map(x=>x.trim()).filter(Boolean),courseIds:$('#pCourses').value.split(',').map(x=>x.trim()).filter(Boolean),featured:$('#pFeatured').checked}})}
async function saveTeacher(){await adminPost('/api/admin/teacher',{x:{id:$('#tId').value||undefined,name:$('#tName').value,bio:$('#tBio').value,photoUrl:$('#tPhoto').value}})}
async function saveClass(){await adminPost('/api/admin/class',{x:{id:$('#clId').value||undefined,title:$('#clTitle').value,exam:$('#clExam').value,date:$('#clDate').value,time:$('#clTime').value,instructor:$('#clInstructor').value,teacherId:$('#clTeacher').value,link:$('#clLink').value,description:$('#clDesc').value}})}
async function saveSettings(){await adminPost('/api/admin/settings',{bankName:$('#sBank').value,accountName:$('#sAccount').value,accountNumber:$('#sNumber').value,ifsc:$('#sIfsc').value,upiId:$('#sUpi').value,supportEmail:$('#sEmail').value,siteName:$('#sSite').value})}
async function adminPost(url,body){try{await api(url,{method:'POST',body:JSON.stringify(body)});toast('Saved successfully','success');await loadAdmin();}catch(e){toast(e.message,'error')}}
async function deleteItem(type,id){if(!confirm('Delete this item?'))return;const map={course:'course',lesson:'lesson',plan:'plan',teacher:'teacher',class:'class'};try{await api(`/api/admin/${map[type]}/${id}/delete`,{method:'POST'});toast('Deleted','success');await loadAdmin()}catch(e){toast(e.message,'error')}}
async function approvePayment(id){try{await api('/api/admin/payment/'+id+'/approve',{method:'POST'});toast('Payment approved','success');await loadAdmin()}catch(e){toast(e.message,'error')}}async function rejectPayment(id){try{await api('/api/admin/payment/'+id+'/reject',{method:'POST'});toast('Payment rejected','success');await loadAdmin()}catch(e){toast(e.message,'error')}}
Object.assign(window,{openLogin,openRegister,login,register,subscribePlan,submitBankPayment,markComplete,approvePayment,rejectPayment,adminSection,saveCourse,saveLesson,savePlan,saveTeacher,saveClass,saveSettings,deleteItem,editCourse,editLesson,editPlan,editTeacher,editClass});
init().catch(e=>{console.error(e);toast('ScorePath could not load. Check the deployment logs.','error')});
